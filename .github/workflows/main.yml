name: .Net 6 CI Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    
jobs:
  build:
  runs-on: ubuntu-latest
  name: CI Build
  steps:
    - name: Code Coverage Summary
      uses: irongut/CodeCoverageSummary@v1.3.0
